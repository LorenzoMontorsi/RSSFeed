<p>
	Ogni articolo nuovo riceve un tag <code>lingua-it</code>, <code>lingua-en</code> e così via.
	Nel menu a sinistra, sotto <strong>Le mie etichette</strong>, compare la lingua corrispondente.
</p>
<p>
	Il riconoscimento usa l'alfabeto e le parole tipiche del testo. I pezzi troppo corti o misti restano senza etichetta, e un'etichetta già assegnata si può correggere a mano dall'articolo.
</p>
<ul>
<?php foreach (LanguageDetector::catalog() as $code => $name) { ?>
	<li><?= htmlspecialchars($name, ENT_QUOTES, 'UTF-8') ?> <code>#lingua-<?= htmlspecialchars($code, ENT_QUOTES, 'UTF-8') ?></code></li>
<?php } ?>
</ul>
